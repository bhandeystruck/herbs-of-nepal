import { PageLoadingSkeleton } from "@/components/ui/page-loading-skeleton";

/**
 * Shared loading UI for public routes.
 */
export default function PublicLoading() {
  return <PageLoadingSkeleton />;
}